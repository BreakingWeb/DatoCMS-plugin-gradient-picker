# Gradient Picker

 A simple gradient picker plugin for creating smooth background gradients. Select a  starting color and adjust its opacity to generate a linear gradient that fades to  white. Perfect for hero sections and page backgrounds. Outputs clean CSS ready to  use in your components.